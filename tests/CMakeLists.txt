add_executable(midi_player_test player_test.cpp ../midiplugin/MIDIPlayer.cpp)
set_target_properties(midi_player_test PROPERTIES AUTOMOC OFF AUTOUIC OFF)
target_include_directories(midi_player_test PRIVATE ../midiplugin)
target_compile_features(midi_player_test PRIVATE cxx_std_20)
target_link_libraries(midi_player_test PRIVATE spessasynth)
add_test(NAME midi_callbacks COMMAND midi_player_test)
if(MIDI_ENABLE_NUKED_SC55)
    target_sources(midi_player_test PRIVATE ../midiplugin/NukedSC55Player.cpp)
    target_link_libraries(midi_player_test PRIVATE fooyin_nuked_sc55)
    target_compile_definitions(midi_player_test PRIVATE MIDI_ENABLE_NUKED_SC55)
    set(MIDI_TEST_ROM_DIR "" CACHE PATH "SC-55 or SC-55mkII ROM directory for optional audio integration tests")
    set(MIDI_TEST_ROM_SET "mk2" CACHE STRING "ROM model for audio integration tests (mk1 or mk2)")
    set_property(CACHE MIDI_TEST_ROM_SET PROPERTY STRINGS mk1 mk2)
    if(NOT MIDI_TEST_ROM_SET STREQUAL "mk1" AND NOT MIDI_TEST_ROM_SET STREQUAL "mk2")
        message(FATAL_ERROR "MIDI_TEST_ROM_SET must be mk1 or mk2")
    endif()
    if(MIDI_TEST_ROM_DIR)
        add_test(NAME midi_nuked_rom COMMAND midi_player_test "${MIDI_TEST_ROM_DIR}" "${MIDI_TEST_ROM_SET}")
    endif()
endif()

if(MIDI_ENABLE_EXTERNAL)
    target_sources(midi_player_test PRIVATE ../midiplugin/ExternalMIDIPlayer.cpp)
    target_compile_definitions(midi_player_test PRIVATE MIDI_ENABLE_EXTERNAL)
endif()
